"use client";

export default function TotalCard() {
  return (
    <div
      className="rounded-2xl p-5 min-h-[80px] border transition-colors duration-300"
      style={{
        backgroundColor: "var(--bg-card)",
        borderColor: "var(--border-light)",
      }}
    />
  );
}
